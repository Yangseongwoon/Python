# 19단표 만들기
for item in range(1, 20):  # 정수 2~19를 가진 리스트를 만ㄷ르어 하나씩 i에 대입합니다.
    for each in range(1, 20):  # 위의 반복문이 실행 될 때마다 이 반복문도 실행됩니다
        print(item, 'X', each, '=', item * each)  # 2 * 2부터 차례대로 계산한 값이 출력됩니다.
        print('%d X %d = %d' % (item, each, item * each))
# 문자열 포매팅으로 print() 함수 더 편하게 사용하기
# print('%d X %d = %d' %(item, each, item * each))
# 여기서 %d는 숫자를 표시할 자리를 정하는 것
# 여기서 %는 %d에 넣을 숫자를 차례대로 지정하는 것
# 첫번째 %d에는 item, 두번째 %d에는 each, 세번째%d에는 item * each
